import React, {memo} from 'react';
import propTypes from 'prop-types';
import './Ticket.css';

const Ticket = memo(function Account(props) {
    return (
        <div className="account">

        </div>
    )
});

Ticket.propTypes = {

};

export default Ticket;
