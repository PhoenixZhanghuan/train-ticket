import React, {memo} from 'react';
import propTypes from 'prop-types';
import './Passengers.css';

const Passengers = memo(function Account(props) {
    return (
        <div className="account">

        </div>
    )
});

Passengers.propTypes = {

};

export default Passengers;
